interface:
  display_name: "DOCX Spec HTML"
  short_description: "Build production spec HTML from DOCX/PDF"
  default_prompt: "Use $docx-spec-html to reconstruct this DOCX/PDF specification into a validated, production-quality single-file HTML page."
