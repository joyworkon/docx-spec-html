interface:
  display_name: "DOCX Spec HTML"
  short_description: "Build production spec HTML from DOCX/PDF"
  default_prompt: "Use $docx-spec-html to reconstruct this DOCX/PDF specification with canonical components, validate DOM contracts, and publish only through finalize_output.py. Use PDF only for hierarchy and table geometry; never hand-author alternate component wrappers or classes."
