interface:
  display_name: "DOCX Spec HTML"
  short_description: "Build production spec HTML from DOCX/PDF"
  default_prompt: "Use $docx-spec-html to reconstruct this DOCX/PDF specification into a validated single-file HTML page. Use PDF only for content hierarchy and table geometry; apply the Skill's canonical rounded-card system with justified body copy and equal image insets."
